# Node-Red Appliance Configuration

# Required: Docker image to use.
# Pin an immutable tag: the generator rejects ':latest' (and untagged images)
# so a rebuilt appliance always contains the exact version it claims.
DOCKER_IMAGE="nodered/node-red:5.0.1"

# Required: Appliance name (single lowercase word, no dashes)
APPLIANCE_NAME="nodered"

# Required: Display name for the application
APP_NAME="Node-Red"

# Required: Publisher information
PUBLISHER_NAME="Pablo del Arco"
PUBLISHER_EMAIL="pdelarco@opennebula.io"

# Required: Application description
APP_DESCRIPTION="Node-RED is a low-code, flow-based programming tool for wiring together devices, APIs, and services"

# Required: Application features (comma-separated)
APP_FEATURES="Visual flow editor,Integrations with devices/APIs,Real-time data transformation,Dashboard & UI building,Extensible via nodes"

# Optional: Container configuration (defaults will be used if not specified)
DEFAULT_CONTAINER_NAME="nodered-app"
DEFAULT_PORTS="1880:1880"
DEFAULT_ENV_VARS=""
DEFAULT_VOLUMES="/data:/data"

# Optional: Application settings
APP_PORT="1880"
WEB_INTERFACE="true"
