# Redis Cache Appliance Configuration
# Simple configuration file for generating a complete Redis appliance

# Required: Docker image to use
DOCKER_IMAGE="redis:alpine"

# Required: Appliance name (single lowercase word, no dashes)
APPLIANCE_NAME="redis"

# Required: Display name for the application
APP_NAME="Redis Cache"

# Required: Publisher information
PUBLISHER_NAME="Your Name"
PUBLISHER_EMAIL="your.email@domain.com"

# Required: Application description
APP_DESCRIPTION="Redis is an in-memory data structure store used as a database, cache, and message broker"

# Required: Application features (comma-separated)
APP_FEATURES="In-memory data storage,High performance caching,Pub/Sub messaging,Data persistence,Atomic operations,Lua scripting"

# Optional: Container configuration
DEFAULT_CONTAINER_NAME="redis-cache"
DEFAULT_PORTS="6379:6379"
DEFAULT_ENV_VARS=""
DEFAULT_VOLUMES="/data:/data"

# Optional: Application settings
APP_PORT="6379"
WEB_INTERFACE="false"
